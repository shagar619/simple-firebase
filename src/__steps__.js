

// 1. go to firebase.google.com and then go to console

// 2. create a firebase project

// 3. go to settings for firebase app and register the app

// 4. npm install firebase

// 5. get firebase config and [you shouldn't] add the config in your project [don't save or share your config]

//6. enable sign in method that you want to use: Build > Authentication > Get Started > Choose options

// 7. go to firebase docs

// 8. create auth (firebase initialize auth instance)

// 9. go to the login button => add event handler and create a provider

// 10. call the signInWithPopup (auth, provider)

// 11. after successfully getting data, you may set it to a state